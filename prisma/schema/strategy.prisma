model Strategy {
  id           BigInt   @id @default(autoincrement())
  ownerUserId BigInt
  name         String
  description  String
  createdAt   DateTime

  owner    User      @relation(fields: [ownerUserId], references: [id])
  channels Channel[]
}