model RolePermission {
  id                Int     @id @default(autoincrement())
  roleId            Int 
  permissionId      Int
  granted           Boolean

  userRoles         Role       @relation(fields: [roleId], references: [id])
  persmissions      Permission @relation(fields: [permissionId], references: [id])
}
