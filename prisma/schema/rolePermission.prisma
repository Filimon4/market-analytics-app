model RolePermission {
  id               Int     @id @default(autoincrement())
  userRoleId       Int
  userPermissionId Int
  granted          Boolean

  userRoles      Role       @relation(fields: [userRoleId], references: [id])
  userPermission Permission @relation(fields: [userPermissionId], references: [id])
}
