model metricToUfChannel {
    id          BigInt          @id @default(autoincrement())
    metricId    BigInt
    ufChannelId BigInt
    metric      metricChannel   @relation(fields: [metricId], references: [id])
    ufChannel   ufChannel       @relation(fields: [ufChannelId], references: [id])
}