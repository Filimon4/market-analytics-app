model User {
  id        BigInt   @id @default(autoincrement())
  name      String
  email     String   @unique
  roleId    Int
  password  String
  createdAt DateTime @default(now())
  statusId  Int

  userToProject UserToProject[]
}
